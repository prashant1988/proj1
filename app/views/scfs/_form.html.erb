<%= form_for(@scf) do |f| %>
  <% if @scf.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@scf.errors.count, "error") %> prohibited this scf from being saved:</h2>

      <ul>
      <% @scf.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :age %><br />
    <%= f.number_field :age %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
